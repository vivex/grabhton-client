import React from 'react';
export default function (resources = [], COMPONENT) {
  // First render the APIHOCWrapper with Component with null/empty obj values
  // Make api calls mentioned in resources
  // wait for all calls
  // Rdnder APIHOCWrapper with data

  return <APIHOCWrapper />
}

class APIHOCWrapper extends React.Component {

  render () {

  }
}